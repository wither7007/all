Object.keys(xy[1]).filter((key) => key.includes('login'))
  reduce((cur, key) => { return Object.assign(cur, { [key]: obj[key] })}, {});


Object.keys(xy[1]).filter((key) => key.includes('name')).reduce((cur, key) => { return Object.assign(cur, { [key]: obj[key] })}, {});
Object.keys(xy[1]).filter((key) => key.includes('u'))
Object.keys(xy[1])

xy.forEach((x,i)=>console.log(i, x.name))
res=[]
xy.forEach((x,i)=>res.push(`${i} ${x.url}         ---   ${x.html_url}`))
copy(res)
